<!doctype html>
<html lang="pt-br">

<head>
  <meta charset="utf-8">
  <title>Catálogo contatos</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    crossorigin="anonymous">

  <style>
    body {
      background: #1d970d;
      color: #e8eaed;
    }

    .navbar,
    .offcanvas {
      background: #0b8146;
    }

    .card {
      background: #0e5514;
      border: 1px solid #0d721b;
      height: 100%;
    }

    .badge-type {
      letter-spacing: .3px;
    }

    .cover {
      width: 100%;
      aspect-ratio: 3/4;
      object-fit: cover;
      background: #0b976d;
    }

    .search-hero {
      background: linear-gradient(180deg, #349b14 0%, #0d9408 100%);
      border-bottom: 1px solid #232833;
    }

    .form-control,
    .form-select,
    .form-button {
      background: #07a06d;
      color: #000000;
      border-color: #068d59;
    }

    .form-button {
      background: #07d835;
    }

    .form-control::placeholder {
      color: #000000;
    }

    .page-link {
      background: #05a34c;
      color: #0a9621;
      border-color: #0ca507;
    }

    .page-item.active .page-link {
      background: #06641d;
      border-color: #066112;
    }

    a,
    a:hover {
      color: #9ad0ff;
    }
  .page-button {
    background-color: #06641d;
    }
  </style>
</head>

<body>
 

</html>